{
  "name": "maetdol",
  "version": "0.1.0",
  "description": "Claude Code productivity plugin — gate, decompose, loop, unstuck",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "prebuild": "npm run typecheck"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  }
}
